{
  "description": "Get the current page URL. Returns the URL as string. Example: {}",
  "name": "web_get_url",
  "parameters": {
    "type": "object",
    "properties": {
      "browser_id": {
        "type": "string",
        "description": "Browser instance ID (returned by web_start). If not specified and only one instance exists, that instance is used."
      }
    },
    "required": []
  }
}