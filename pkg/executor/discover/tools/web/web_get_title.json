{
  "description": "Get the current page title. Example: {}",
  "name": "web_get_title",
  "parameters": {
    "type": "object",
    "properties": {
      "browser_id": {
        "type": "string",
        "description": "Browser instance ID (returned by web_start). If not specified and only one instance exists, that instance is used."
      }
    },
    "required": []
  }
}