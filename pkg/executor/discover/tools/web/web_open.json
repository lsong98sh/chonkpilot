{
  "description": "Navigate to a URL in the browser. Waits for page load. Returns: 'navigated to {url}, title: {page_title}'. Example: {\"url\": \"https://example.com\"}",
  "name": "web_open",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "URL to navigate to"
      },
      "browser_id": {
        "type": "string",
        "description": "Browser instance ID (returned by web_start). If not specified and only one instance exists, that instance is used."
      }
    },
    "required": [
      "url"
    ]
  }
}