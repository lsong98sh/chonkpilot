{
  "description": "Move the mouse to absolute page coordinates. Returns confirmation. Example: {\"x\": 500, \"y\": 300}",
  "name": "web_mouse_move",
  "parameters": {
    "type": "object",
    "properties": {
      "x": {
        "type": "number",
        "description": "Target X coordinate"
      },
      "y": {
        "type": "number",
        "description": "Target Y coordinate"
      },
      "modifiers": {
        "type": "array",
        "description": "Keyboard modifiers"
      },
      "browser_id": {
        "type": "string",
        "description": "Browser instance ID (returned by web_start). If not specified and only one instance exists, that instance is used."
      }
    },
    "required": [
      "x",
      "y"
    ]
  }
}