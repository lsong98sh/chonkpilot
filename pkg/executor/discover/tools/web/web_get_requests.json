{
  "description": "Get network request history from the page. Returns list of requested URLs and their status. Example: {}",
  "name": "web_get_requests",
  "parameters": {
    "type": "object",
    "properties": {
      "browser_id": {
        "type": "string",
        "description": "Browser instance ID (returned by web_start). If not specified and only one instance exists, that instance is used."
      }
    },
    "required": []
  }
}