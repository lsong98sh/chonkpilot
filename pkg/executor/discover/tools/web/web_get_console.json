{
  "description": "Get accumulated console log messages from the page. Example: {}",
  "name": "web_get_console",
  "parameters": {
    "type": "object",
    "properties": {
      "browser_id": {
        "type": "string",
        "description": "Browser instance ID (returned by web_start). If not specified and only one instance exists, that instance is used."
      }
    },
    "required": []
  }
}