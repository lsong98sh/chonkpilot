{
  "description": "Start a headless Chrome browser instance. Chrome is auto-discovered via PATH, registry, and common install paths (no external process manager). Returns: 'browser started (headless: true/false)'. Example: {\"headless\": true}",
  "name": "web_start",
  "parameters": {
    "type": "object",
    "properties": {
      "headless": {
        "type": "boolean",
        "description": "Run headless (default true). Set false to see the browser window"
      }
    },
    "required": []
  }
}