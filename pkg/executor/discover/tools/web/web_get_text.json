{
  "description": "Get text content of an element by CSS selector. Example: {\"selector\": \"h1\"}",
  "name": "web_get_text",
  "parameters": {
    "type": "object",
    "properties": {
      "selector": {
        "type": "string",
        "description": "CSS selector"
      },
      "browser_id": {
        "type": "string",
        "description": "Browser instance ID (returned by web_start). If not specified and only one instance exists, that instance is used."
      }
    },
    "required": [
      "selector"
    ]
  }
}