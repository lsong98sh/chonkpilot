{
  "description": "Apply a unified diff to a file. Returns: 'patch applied to {target} ({N} bytes written)'. Example: {\"target\": \"main.go\", \"diff\": \"--- a/main.go\\n+++ b/main.go\\n@@ -1,3 +1,4 @@\\n+// comment\\n\"}",
  "name": "patch",
  "parameters": {
    "type": "object",
    "properties": {
      "target": {
        "type": "string",
        "description": "Target file path"
      },
      "diff": {
        "type": "string",
        "description": "Unified diff content to apply"
      }
    },
    "required": [
      "target",
      "diff"
    ]
  }
}