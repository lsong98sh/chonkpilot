{
  "description": "Get details of a specific custom tool. Returns name, type, description, source, command (if any), and parameter schema. Example: {\"name\": \"my_tool\"}",
  "name": "get_tool",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Tool name to look up"
      }
    },
    "required": [
      "name"
    ]
  }
}